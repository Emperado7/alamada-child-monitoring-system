<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main theme — no action bar, green status bar -->
    <style name="Theme.AlamadaChildMonitor"
        parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <!-- Splash theme -->
    <style name="Theme.AlamadaChildMonitor.Splash"
        parent="Theme.AlamadaChildMonitor">
        <item name="android:windowBackground">@drawable/header_green_gradient</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

    <!-- Login — white background -->
    <style name="Theme.AlamadaChildMonitor.Login"
        parent="Theme.AlamadaChildMonitor">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

</resources>
